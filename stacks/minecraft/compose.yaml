# Generated by setupmc.com on 2025-07-13T19:36:04.146Z
services:
  mc:
    image: itzg/minecraft-server:latest
    container_name: minecraftServer
    restart: unless-stopped
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      LEVEL: "${LEVEL}"
      MEMORY: "${MEMORY}"
      MOTD: "${MOTD}"
      TZ: "Europe/Prague"
      OPS: "${OPS}"
      EXISTING_OPS_FILE: "SYNCHRONIZE"
    volumes:
      - ${MINECRAFT_DATA_DIR:-/mnt/data-sync/minecraft}:/data